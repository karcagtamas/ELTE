train: ../dataset/train/images    # Path to training images directory
  - ../dataset/train/labels       # Path to training labels directory (YOLO format)
val: ../dataset/val/images        # Path to validation images directory
  - ../dataset/val/labels         # Path to validation labels directory (YOLO format)
test: ../dataset/test/images      # Path to test images directory (optional)
  - ../dataset/test/labels        # Path to test labels directory (optional, if testing)
  
nc: 3
names: ['Car.jpg', 'Diver.jpg', 'Rubiks_Cube.jpg'] 
